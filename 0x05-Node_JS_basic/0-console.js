// Defining the function
const displayMessage = (message) => {
  console.log(message); // Printing the message to STDOUT
};
// Calling the function with a sample message
module.exports = displayMessage;
